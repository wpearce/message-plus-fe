import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'mp-registration-guide',
  imports: [RouterOutlet],
  template: '<router-outlet />',
})
export default class RegistrationGuideComponent {}
